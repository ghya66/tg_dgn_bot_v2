# ============================================
# Telegram Bot 环境变量配置示例
# 复制此文件为 .env 并填写实际值
# ============================================

# ========== 必填项 ==========

# Telegram Bot Token (从 @BotFather 获取)
BOT_TOKEN=your_bot_token_here

# USDT TRC20 收款地址 (波场网络)
USDT_TRC20_RECEIVE_ADDR=T...your_trc20_address_here

# Webhook 签名密钥 (用于验证支付回调)
WEBHOOK_SECRET=your_webhook_secret_here

# ========== 管理员配置 ==========

# Bot 管理员 Telegram ID (用于访问 /admin 面板)
BOT_OWNER_ID=123456789

# ========== 运行模式 ==========

# 是否使用 Webhook 模式 (生产环境建议 true)
USE_WEBHOOK=false

# Bot 服务监听地址和端口
BOT_SERVICE_HOST=0.0.0.0
BOT_SERVICE_PORT=8080

# Webhook URL (USE_WEBHOOK=true 时需要)
# BOT_WEBHOOK_URL=https://your-domain.com/webhook

# ========== Redis 配置 ==========

# 方式1: 独立配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# 方式2: 连接字符串 (Zeabur 等云平台自动注入)
# REDIS_CONNECTION_STRING=redis://user:pass@host:port/db

# ========== 数据库配置 ==========

# SQLite 数据库路径
DATABASE_URL=sqlite:///./data/tg_bot.db

# ========== TRX 兑换配置 ==========

# 收 USDT 地址
TRX_EXCHANGE_RECEIVE_ADDRESS=T...your_receive_address

# 发 TRX 地址
TRX_EXCHANGE_SEND_ADDRESS=T...your_send_address

# 发 TRX 私钥 (生产环境必填)
# TRX_EXCHANGE_PRIVATE_KEY=your_private_key

# 收款二维码 Telegram file_id
# TRX_EXCHANGE_QRCODE_FILE_ID=AgACAgIA...

# 默认汇率 (1 USDT = X TRX)
TRX_EXCHANGE_DEFAULT_RATE=3.05

# 测试模式 (不实际转账)
TRX_EXCHANGE_TEST_MODE=true

# ========== 能量服务配置 ==========

# 能量 API 账号 (对接 trxfast.com / trxno.com)
# ENERGY_API_USERNAME=your_username
# ENERGY_API_PASSWORD=your_password

# 能量 API 地址 (主地址和备用地址)
# ENERGY_API_BASE_URL=https://trxno.com
# ENERGY_API_BACKUP_URL=https://trxfast.com

# 能量代理地址 (TRX 直转模式)
# ENERGY_RENT_ADDRESS=T...
# ENERGY_PACKAGE_ADDRESS=T...
# ENERGY_FLASH_ADDRESS=T...

# ========== TRON API 配置 (可选) ==========

# Tron API URL 和密钥
# TRON_API_URL=https://api.trongrid.io
# TRON_API_KEY=your_api_key

# 区块链浏览器 (tronscan | oklink)
TRON_EXPLORER=tronscan

# ========== 其他配置 ==========

# 订单超时时间 (分钟)
ORDER_TIMEOUT_MINUTES=30

# 地址查询限频 (分钟)
ADDRESS_QUERY_RATE_LIMIT_MINUTES=1

# USDT 汇率缓存 TTL (秒)
USDT_RATES_CACHE_TTL=3600
